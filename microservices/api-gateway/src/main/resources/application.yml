spring:
  application:
    name: api-gateway
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${keycloak.issuer-uri}
  config.import: "consul:"
  cloud:
    consul:
      host: ${CONSUL_HOST}          # ????? Consul (?? ????????? localhost)
      port: ${CONSUL_PORT}         # ???? Consul (8500 ?? ?????????)
      discovery:
        service-name: api-gateway  # ?????????????, ?? ????????? ??????? spring.application.name
        ip-address: ${app.ip-address}
        prefer-ip-address: true
      config:
        enabled: true            # ???????? Consul Config ??? ??????? ????????????
        prefix: config           # ??????? ? KV-????????? Consul (?? ????????? "config")
        defaultContext: application  # ???????? ?? ?????????
        format: YAML
#  gateway:
#    routes:
#      - id: accounts-route  # ID ?????
#        uri: lb://accounts-service  # URI ????????????, ?? ??????? ??????? ?????????????? ??????
#        predicates:
#          - Path=/accounts/** # URI ???????????? ? Gateway ???????, ? ??????? ???????? ????
#        filters:
#          - RewritePath=/accounts/?(?<segment>.*), /$\{segment} # ??????, ??????? ???????????? ?????? ? ???????? ??????? (????????? ??? ??????? ? ??????)
#          - TokenRelay= # ??? ???????? ??????
logging:
  level:
    root: INFO
    reactor.netty: DEBUG
    org:
      springframework:
        security: DEBUG
        cloud.gateway: DEBUG
        http.server.reactive: DEBUG
        web.reactive: DEBUG